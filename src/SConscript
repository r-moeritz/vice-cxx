import platform
import os

ccflags = '-std=c++11 -Wall -Wextra -g'
ldflags = '--static'
cpppath = ['../include']
source  = ['main.cc', 'init.cc', 'bitboards.cc']
target = 'vice++'

if platform.system() == 'Windows':
    env = Environment(tools   = ['mingw'],
                      CCFLAGS = ccflags,
                      CPPPATH = cpppath,
                      ENV     = os.environ)
else:
    env = Environment(CCFLAGS = ccflags,
                      CPPPATH = cpppath,
                      ENV     = os.environ)

env.Program(target    = target, 
            source    = source,
            LINKFLAGS = ldflags)
